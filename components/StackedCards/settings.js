export const stackedOptions = 'Top'; // Change stacked cards view from 'Bottom', 'Top' or 'None'.
export const rotate = true; // Activate the elements' rotation for each move on stacked cards.
export const items = 3; // Number of visible elements when the stacked options are bottom or top.
export const elementsMargin = 40; // Define the distance of each element when the stacked options are bottom or top.
export const useOverlays = false; // Enable or disable the overlays for swipe elements.
export const velocity = 0.3; // Minimum velocity allowed to trigger a swipe.
export const scaleCardBy = 0.2; // scale size factor
export const opacityDifferenceFactor = 0.15; // opacity factor
